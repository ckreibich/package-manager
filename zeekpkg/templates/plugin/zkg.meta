[package]
script_dir = scripts
build_command = cd plugin && ./configure && cd build && make
test_command = cd testing && btest -c btest.cfg
summary = TODO: A summary of @PACKAGE_NAME@ in one line
description = TODO: A more detailed description of @PACKAGE_NAME@.
    It can span multiple lines, with this indentation.
